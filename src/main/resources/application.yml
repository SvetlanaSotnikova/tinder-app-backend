
server:
  port: 8080

#jwt:
#  secret: ${JWT_SECRET}

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tinderdb
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect



